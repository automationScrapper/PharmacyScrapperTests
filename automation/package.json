{
  "name": "playwright-automation",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright tests triggered by Go API",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke",
    "list": "playwright test --list",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
